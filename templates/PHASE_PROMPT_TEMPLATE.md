# Phase Prompt Template — Lab Tools 2

> **Copy this file when starting a new phase. Fill every section. Delete bracketed instructions before sending to the agent.**
>
> Sections marked **[BLOCKING]** are mandatory — the phase fails review if they're skipped.

---

## Phase Identity

**Phase name:** [e.g. "Tiptap Migration Phase 4 — toolbar refactor"]
**Plan doc:** [e.g. `plans/TIPTAP-MIGRATION.md` § "Phase 4"]
**Agent assignment:** [Claude Code Pro / Antigravity Sonnet default / Antigravity Sonnet high effort / Antigravity Opus high effort]
**Rationale for agent:** [one line — why this agent for this phase]

## Scope

**In scope (this phase will modify):**
- [explicit list of files or file patterns]

**Out of scope (do NOT modify):**
- [explicit list — anything tempting but not for this phase]

**Definition of done:**
- [observable outcome 1]
- [observable outcome 2]
- All tests pass: `cd backend && pytest && cd ../frontend && npx vitest run && npx tsc --noEmit`
- `make check-index` exits clean

---

## [BLOCKING] Pre-flight gate

Run these commands BEFORE writing any code. If any step fails, STOP and surface the failure to the user. Do NOT attempt fixes autonomously.

```bash
cd $(git rev-parse --show-toplevel)
git status                    # working tree must be clean
make check-index              # CODEBASE_INDEX.md must be fresh
git log -1 --format=%H        # record starting commit for postflight diff
```

**Stop conditions (each is a hard fail-stop):**
1. `git status` shows uncommitted changes → ask user how to proceed
2. `make check-index` exits non-zero → a previous phase bypassed the hook; surface the diff and stop
3. The user's task description references files NOT listed in `## Scope` → confirm scope before proceeding

---

## [BLOCKING] Ground truth

**The following blocks are auto-generated facts copied from `CODEBASE_INDEX.md`. Treat them as the only authoritative source for current model/route/hook shape. Any older documentation that conflicts with these blocks is wrong — flag the conflict, do not silently reconcile.**

### Models in scope
[Paste relevant `### \`ModelName\`` sections from CODEBASE_INDEX.md verbatim.]

### Routes in scope
[Paste relevant rows from the "Resolved API Routes" table.]

### Schemas in scope
[Paste relevant schema sections.]

### Frontend symbols in scope
[Paste relevant hooks and component rows from the frontend index.]

---

## Design intent

[1–3 paragraphs explaining WHY this phase exists, not what it does. Cite ARCHITECTURE.md sections by name. If design intent in ARCHITECTURE.md is wrong or outdated, flag it — do not work around it silently.]

---

## Implementation steps

[Numbered steps, each ending in a logical commit point. Reference exact file paths and symbol names from the ground-truth section above.]

1. [step]
   - **Commit:** "[exact commit message]"
2. [step]
   - **Commit:** "[exact commit message]"

---

## [BLOCKING] Doc updates within this phase

The phase is INCOMPLETE without addressing every item below. If a doc does not need an update, write a one-line justification rather than skipping silently.

- **`ARCHITECTURE.md`** — update if design intent changed.
- **`CONVENTIONS.md`** — update if a new rule emerged from a "decision under discretion" or a clarification needed.
- **`<plan-doc>.md`** — mark this phase complete; append "Lessons learned" if any pattern surprised you.
- **`CODEBASE_INDEX.md`** — auto-regenerated by pre-commit. Do NOT hand-edit.

---

## [BLOCKING] Post-flight verification

After all implementation commits but BEFORE handing back:

```bash
make index
git diff <STARTING_COMMIT_FROM_PREFLIGHT> -- CODEBASE_INDEX.md
```

**Verify:**
1. The index diff contains only changes to symbols listed in `## Scope`. Any symbol added/removed outside scope is a bug — STOP and surface it.
2. All tests pass (backend + frontend + tsc).
3. `git log` shows exactly the commits described in `## Implementation steps`.
4. No `--no-verify` was used.

---

## Forbidden patterns

- "Let me check what `<ModelName>` looks like by reading `models.py`" → use the ground-truth block. If not there, the prompt is incomplete — ask the user.
- "I'll add a quick fix to `<file outside Scope>`" → out of scope is out of scope.
- "The docs say X but the code says Y, I'll silently follow the code" → flag the conflict explicitly.
- `git commit --no-verify` → never. The hook is the contract.
- "I'll regenerate `CODEBASE_INDEX.md` by hand" → only the script generates the index.
- Applying the Python no-f-string-without-placeholders rule to TypeScript template literals — that rule is Python-specific. Use template literals freely in TS/TSX.

---

## [BLOCKING] Reporting

Paste this verbatim back to the user when complete. Fill every field. The `REUPLOAD_REQUIRED:` block is machine-parsed — do not add prose inside it.

```
Phase <name> — completed | failed | stopped at <step>

Pre-flight: <pass | details>

Files changed:
<list>

Implementation notes:
<bullet list of decisions, surprises, file structure observations>

Test results:
- npx tsc --noEmit: pass | fail
- backend pytest: pass | fail
- full vitest run: pass | fail (was N, now M)

Commit SHA(s): <from git log --oneline>

Decisions made under discretion:
<free-form — INCLUDE every place CC made a judgment call without explicit instruction>

Anomalies / surprises:
<free-form>
```

---

## [BLOCKING] Project knowledge re-upload manifest

After the report block above, append the manifest. Project knowledge contents (the only files eligible for this list):

- `ARCHITECTURE.md`
- `CLAUDE.md`
- `CLAUDE_WEB.md`
- `CONVENTIONS.md`
- `FRONTEND-CONVENTIONS.md`
- `CODEBASE_INDEX.md`
- `PHASE_PROMPT_TEMPLATE.md`
- Any file under `plans/` (active plan docs)

Source files (`.py`, `.ts`, `.tsx`, `.css`) are NOT in project knowledge — do NOT list them.

Format strictly (machine-parsed by web Claude — exact header text required):

```
REUPLOAD_REQUIRED:
- <repo-relative path>
- <repo-relative path>

REUPLOAD_NOT_NEEDED: <reason — required even when list is non-empty>
```

If no project-knowledge files were modified, output exactly:

```
REUPLOAD_REQUIRED: (none)
REUPLOAD_NOT_NEEDED: phase modified only source; no docs touched
```

**Stop here. Do not push to origin. Do not start the next phase.**
